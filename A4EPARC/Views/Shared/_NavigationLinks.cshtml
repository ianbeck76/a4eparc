<ul class="nav navbar-nav navbar-right">
    <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Account<b class="caret"></b></a>
        <ul class="dropdown-menu">
            @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "", Area = "" }))
            {
                @Html.AntiForgeryToken()
            }
            <li><a href="javascript:document.getElementById('logoutForm').submit()"><i class="fa fa-sign-out"></i> Log out</a></li>
            @if (HttpContext.Current.Request.IsAuthenticated)
            {
                <li><a href="@Url.Action("ChangePassword", "Account", new { Area = "" })"><i class="fa fa-key"></i> Change Password</a></li>
            }
            @if (Context.User.IsInRole("IsAdmin"))
            {
                <li><a href="@Url.Action("Index", "User", new { Area = "" })"><i class="fa fa-edit"></i> User Admin </a></li>
                <li><a href="@Url.Action("Index", "SiteLabels", new { Area = "" })"><i class="fa fa-edit"></i> Site Admin </a></li>
                <li><a href="@Url.Action("Index", "QuestionAdmin", new { Area = "" })"><i class="fa fa-edit"></i> Questions Admin </a></li>
            }
        </ul>
    </li>
</ul>